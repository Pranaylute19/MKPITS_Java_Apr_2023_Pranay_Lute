//write a C program that accept an employeeIDs totl worked hours of month and the amount he received per hous. print the employee id and salary of a particular month.
#include<stdio.h>
#include<conio.h>

void main(){
clrscr();
int id,hour,value,salary;
printf("enter the value");
scanf("%d%d",&id,&value);
salary=hour*value;
printf("%d",salary);
getch();
}
