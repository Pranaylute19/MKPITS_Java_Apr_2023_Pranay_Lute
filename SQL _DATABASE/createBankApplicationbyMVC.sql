create database BankApplicationMVC
use BankApplicationMVC

create table customer(
	User_id varchar(20),
     password varchar(20),
    name varchar(20),
    Address varchar(20),
    City varchar(20)
)

select * from customer
